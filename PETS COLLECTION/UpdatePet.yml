info:
  name: UpdatePet
  type: http
  seq: 5

http:
  method: PUT
  url: "{{ServUrl}}/pets/1"
  body:
    type: json
    data: |-
      {
        "name": "Mopson",
        "age": 5,
        "kind": "dog",
        "breed": "pug"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
