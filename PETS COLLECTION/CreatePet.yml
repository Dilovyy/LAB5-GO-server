info:
  name: CreatePet
  type: http
  seq: 3

http:
  method: POST
  url: "{{ServUrl}}/pets"
  body:
    type: json
    data: |-
      {
        "name": "Gazik",
        "age": 3,
        "kind": "dog",
        "breed": "chihuahua"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
